import { FlowDirective } from './flow.directive';

describe('FlowDirective', () => {
  it('should create an instance', () => {
    const directive = new FlowDirective();
    expect(directive).toBeTruthy();
  });
});
